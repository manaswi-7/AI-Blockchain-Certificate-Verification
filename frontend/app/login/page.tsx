"use client";
import {FormEvent,useState} from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8080/api";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [msg,setMsg]=useState("");async function submit(e:FormEvent){e.preventDefault();setMsg("Signing in…");try{const r=await fetch(`${API}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw Error(d.error);localStorage.setItem("token",d.token);localStorage.setItem("role",d.role);window.location.href="/dashboard"}catch(err){setMsg(err instanceof Error?err.message:"Login failed")}}return <main className="hero" style={{maxWidth:520}}><div><p className="eyebrow">ISSUER PORTAL</p><h1 style={{fontSize:46}}>Sign in</h1><form onSubmit={submit} className="verify" style={{display:"block"}}><input style={{width:"100%",marginBottom:10}} type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input style={{width:"100%",marginBottom:10}} type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button style={{width:"100%"}}>Continue</button></form><p>{msg}</p><a href="/">← Back to verification</a></div></main>}
