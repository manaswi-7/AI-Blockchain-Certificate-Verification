"use client";
import {useEffect,useState} from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"http://localhost:8080/api";
export default function Verify({params}:{params:{certificateId:string}}){const [d,setD]=useState<any>(null);const [file,setFile]=useState<File|null>(null);const [msg,setMsg]=useState("");async function check(){const r=await fetch(`${API}/verify/${encodeURIComponent(params.certificateId)}`);setD(await r.json())}useEffect(()=>{check()},[]);async function upload(){if(!file)return;setMsg("Checking document…");const f=new FormData();f.append("certificateId",params.certificateId);f.append("file",file);const r=await fetch(`${API}/verify/upload`,{method:"POST",body:f});const x=await r.json();setD(x);setMsg(r.ok?"Verification complete":(x.message||x.error||"Verification failed"))}return <main className="page"><nav><b>CertiChain</b><a href="/">Home</a></nav><section style={{maxWidth:760,margin:"50px auto",padding:"0 24px"}}><h1>Verify Certificate</h1><p>Certificate ID: <b>{params.certificateId}</b></p>{d&&<div className="verify" style={{marginTop:24}}><h2>{d.result}</h2><p>{d.reason}</p>{d.certificate&&<><p><b>Recipient:</b> {d.certificate.recipient_name}</p><p><b>Course:</b> {d.certificate.course}</p><p><b>Institution:</b> {d.certificate.institution}</p><p><b>Document hash:</b> {d.certificate.document_hash}</p><p><b>Blockchain:</b> {d.blockchainMatch?"Verified":"Not verified"}</p>{d.certificate.qr_code&&<img src={d.certificate.qr_code} alt="Certificate QR code" width={180}/>}</>}</div>}<h2 style={{marginTop:40}}>Verify the actual certificate file</h2><input type="file" accept=".pdf,.png,.jpg,.jpeg" onChange={e=>setFile(e.target.files?.[0]||null)}/><button onClick={upload} disabled={!file} style={{marginTop:14}}>Verify uploaded certificate</button><p>{msg}</p></section></main>}
